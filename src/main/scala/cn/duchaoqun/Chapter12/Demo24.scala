package cn.duchaoqun.Chapter12

object Demo24 extends App {
  // 在 Scala 中，每当你想要告诉另外一个函数做某事时，你都会传入一个“函数参数”给它。

  // 在 Java 中，需要付出更多才能达到相同的效果。
  // 通常的做法是将动作放在一个实现某接口的类中，然后将该类的一个对象传递给另外一个方法。
  // 在很多时候，这些接口都只有单个抽象方法（single abstract method）。
  // 在 Java 中，他们被称作 SAM 类型。

}
